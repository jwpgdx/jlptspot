services:
  - type: web
    name: jlpt-monitor
    env: node
    plan: free
    buildCommand: npm install && npx playwright install --with-deps
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
